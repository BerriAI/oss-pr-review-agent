# Slack bot token (xoxb-...) with chat:write and app_mentions:read scopes.
# From your Slack app's "OAuth & Permissions" page.
SLACK_BOT_TOKEN=""

# Slack signing secret. From your Slack app's "Basic Information" → "App Credentials" page.
# Used to verify that incoming events actually came from Slack.
SLACK_SIGNING_SECRET=""

# LiteLLM proxy virtual key (or upstream provider key if hitting a provider directly).
LITELLM_API_KEY=

# Optional: LiteLLM proxy base URL. Defaults to http://0.0.0.0:4000 (local proxy).
# Point at your hosted proxy in prod, e.g. https://your-litellm-proxy.com
LITELLM_API_BASE=

# Optional: model alias to call through the proxy. Defaults to claude-sonnet-4-6.
# Must match a model name your LiteLLM proxy is configured to route.
LITELLM_MODEL=

# GitHub PAT used by the litellm-pr-reviewer skill. Needs public_repo scope (or repo for private).
# Without this the skill fails: GitHub's 60 req/hr unauthenticated quota 403s partway through.
GITHUB_TOKEN=

# CircleCI failure log tails are fetched via the LiteLLM proxy's MCP endpoint
# (`{LITELLM_API_BASE}/mcp/`, `circle_ci_mcp-get_build_failure_logs` tool), so
# no CircleCI token is needed locally — the proxy holds it. Set LITELLM_API_BASE
# and LITELLM_API_KEY above to enable; without them, only GitHub's check-run
# summaries are used.

# Optional: admin login for the /chat dev UI. Set BOTH to gate /chat behind a
# username+password form. If unset, /chat is wide open (intended for local dev).
# When set, also set SESSION_SECRET (otherwise a random one is generated per
# process and sessions die on restart).
ADMIN_USERNAME=
ADMIN_PASSWORD=
SESSION_SECRET=

# Optional: bearer-token auth for programmatic clients (the litellm-loop skill,
# CI bots, etc.) that hit /chat/api without a browser. CSV so multiple keys can
# co-exist during rotation. Each key is a freeform string; mint with
# `openssl rand -base64 32`. Clients pass `Authorization: Bearer <key>`.
# When BOT_API_KEYS is set (with or without ADMIN_USERNAME/ADMIN_PASSWORD),
# /chat/api requires a valid bearer OR a valid session cookie.
BOT_API_KEYS=

# Optional: hosting platforms set this. Defaults to 8000.
PORT=

# Optional: Pydantic Logfire write token. When set, agent runs, HTTPX calls,
# FastAPI requests, and the gather subprocess are traced and shipped to Logfire.
# Without it, instrumentation is a no-op (send_to_logfire="if-token-present").
# Create one at https://logfire.pydantic.dev → project → Settings → Write tokens.
LOGFIRE_TOKEN=
